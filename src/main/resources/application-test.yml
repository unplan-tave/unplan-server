spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/unplan
    username: unplan
    password: unplan
    driver-class-name: org.postgresql.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
    show-sql: true